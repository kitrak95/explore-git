#include "math-lib.h"

//You are required to return an integer array with the factors of the given number
//You may also need to convert the given double number to an integer
double ml_factorise(double n1)
{
    return 0;
}
