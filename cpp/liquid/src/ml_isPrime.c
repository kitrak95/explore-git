#include "math-lib.h"

//If the given number is of type double, round off the number and convert it into an integer before proceeding.
//1 for prime, 0 for composite
int ml_isPrime(double n1)
{
    return 0;
}

